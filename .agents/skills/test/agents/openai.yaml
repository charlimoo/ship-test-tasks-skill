interface:
  display_name: "Testcase Commands"
  short_description: "Run /test, /bugfix, /creds"
  default_prompt: "Use $test to run /test, /bugfix, or /creds with the internal Testcase app."

policy:
  allow_implicit_invocation: true
